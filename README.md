# aiml
The repository contains all the problems worked out during the course of learning AI concepts with IITM


Command to link notebook with venv 
`python -m ipykernel install --user --name ai_env --display-name "Python (ai_env)"`